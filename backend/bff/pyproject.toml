[project]
name = "bff"
version = "0.1.0"
description = "Backend for Frontend layer for Livny Health EHR"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "httpx>=0.28.0",
]
